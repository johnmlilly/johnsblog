// Recommended way, to include only the icons you need.
import { createIcons, Copy , CircleCheck, Mail, } from 'lucide';

createIcons({
  icons: {
        Copy,
        CircleCheck,
        Mail,
  }
});